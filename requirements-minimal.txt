# Minimal dependencies - no Docker, no external services required
# For local development and testing

# ============================================
# CORE (Required)
# ============================================
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# ============================================
# SCRAPING (Choose one or more)
# ============================================
# Lightweight option
beautifulsoup4>=4.12.2
lxml>=4.9.3
requests>=2.31.0
httpx>=0.25.2

# For JavaScript sites (optional)
playwright>=1.40.0

# ============================================
# DATABASE (SQLite - built into Python)
# ============================================
aiosqlite>=0.19.0  # Async SQLite
sqlalchemy>=2.0.23

# ============================================
# VECTOR SEARCH (ChromaDB - no PostgreSQL needed)
# ============================================
chromadb>=0.4.0

# ============================================
# EMBEDDINGS (Local)
# ============================================
sentence-transformers>=2.2.2

# ============================================
# UTILITIES
# ============================================
tenacity>=8.2.3  # Retry logic
tqdm>=4.66.1  # Progress bars

# ============================================
# OPTIONAL (Add when needed)
# ============================================
# celery>=5.3.4  # Task queue (only if scaling)
# redis>=5.0.1  # Cache (only if needed)
# spacy>=3.7.2  # NER (only for knowledge graph)
